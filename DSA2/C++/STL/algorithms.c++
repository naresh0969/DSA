/*Algorithms in c++
    binary search*/
#include<iostream>
#include<algorithm>
#include<vector>
using namespace std;
int mian(){
    vector<int>v;
    v.push_back(4);
    v.push_back(1);
    v.push_back(8);
    v.push_back(3);

    return 0;
}